overwrite: true
schema: 'schema.graphql'
documents:
    - 'src/tests/**/*.gql.ts'
generates:
    ./src/tests/integration/types/generated.ts:
        plugins:
            - typescript
            - typescript-operations
        hooks:
            afterOneFileWrite:
                - prettier --write
